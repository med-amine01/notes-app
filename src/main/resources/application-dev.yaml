spring:
  datasource:
    url: jdbc:h2:mem:notesdb
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    com.app.notes: DEBUG

